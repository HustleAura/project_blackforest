// chats(collection) -> personEmail(document) -> contact
const String Chats_Collection = 'chats';

// messages(collection) -> messageId(documents) -> (message,sender,receiver)
const String Message_Collection = 'messages';
const String Contact_Collection = 'contacts';
const String User_Collection = 'user';

const String Error_Message = 'Something went wrong. Please try again later.';
